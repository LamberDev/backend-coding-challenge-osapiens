# SQLite database file path (relative to project root)
DB_PATH=data/database.sqlite

# Drop and recreate the database schema on every start
DB_DROP_SCHEMA=true

# Auto-sync TypeORM entity definitions to the DB schema on start
DB_SYNCHRONIZE=true

# How often (in milliseconds) the task worker polls for queued tasks
WORKER_POLL_INTERVAL_MS=5000